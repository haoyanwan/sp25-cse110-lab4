1. values added:  20
2. final result:  20
3. We should not use var since it is named across the whole function instead of just the block, causing issues
4. values added:  20
5. The code throws an error because let is block based instead of function based.
6. The code throws an error because you cannot reassign a constant value
7. The code throws an error because the constant keyword is in the wrong scope

